# ============================================================================
# Feed-Exporter Dependencies - Cron Job Mode
# ============================================================================
#
# RIMOSSO (non più necessario per Cron Job):
#   - flask (web server)
#   - gunicorn (WSGI server)
#   - APScheduler (scheduling interno)
#
# Scheduling ora gestito da:
#   - Render Cron Job (infrastruttura)
#   - Scheduler app (trigger esterni)
#
# ============================================================================

# HTTP Client per Shopify API
requests==2.31.0

# Local package installation (per import src.*)
-e .
